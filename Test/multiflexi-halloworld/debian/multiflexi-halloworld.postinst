#!/bin/sh
# postinst script for multiflexi-halloworld
set -e

# Automatically added by dh_installdeb
#DEBHELPER#

APPLICATION=multiflexi-halloworld
if [ -d "/usr/lib/$APPLICATION/multiflexi" ]; then
  for file in /usr/lib/$APPLICATION/multiflexi/*.json; do
    [ -f "$file" ] || continue
    multiflexi-json2app "$file" || true
  done
fi
